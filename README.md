# cloudcons/homebrew-tap

```console
$ brew install cloudcons/tap/deconflict
```

| Formula      | Source                                                              |
| ------------ | ------------------------------------------------------------------- |
| `deconflict` | [cloudcons/deconflict-cli](https://github.com/cloudcons/deconflict-cli) |

Formulae here follow each project's latest release: `.github/workflows/update.yml`
checks every half hour and regenerates them, so direct edits are overwritten by
the next release.
